apiVersion: v1
kind: Service
metadata:
  name: service-track-catalogo
  labels:
    app: service-track-catalogo
spec:
  type: ClusterIP
  selector:
    app: service-track-catalogo
  ports:
    - name: http
      port: 80
      targetPort: 8080
